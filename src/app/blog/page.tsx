import Link from "next/link"; import { ArrowRight, CalendarDays } from "lucide-react"; import { db } from "@/lib/db"; import Photo from "@/components/site/Photo";
export const dynamic="force-dynamic";
export const metadata={title:"Journal",description:"Stories, guides and ideas from Vrindavan Holiday Inn."};
export default async function BlogPage(){const posts=await db.blog.findMany({where:{published:true},orderBy:[{featured:"desc"},{publishedAt:"desc"},{createdAt:"desc"}]});return <div className="blog-page"><section className="blog-hero"><div className="home-shell"><span className="home-eyebrow">VHI JOURNAL</span><h1>Stories from Vrindavan</h1><p>Local guides, thoughtful travel ideas and the little details that make a stay feel like home.</p></div></section><main className="home-shell py-14"><div className="blog-grid">{posts.map((p:any)=><Link key={p.id} href={`/blog/${p.slug}`} className="blog-card"><div className="blog-card-image"><Photo src={p.coverImage} alt={p.title} className="h-full w-full"/></div><div className="blog-card-copy"><div className="flex items-center justify-between gap-3"><span className="home-eyebrow">{p.category}</span>{p.publishedAt&&<small><CalendarDays size={13}/> {new Date(p.publishedAt).toLocaleDateString("en-IN",{day:"numeric",month:"short",year:"numeric"})}</small>}</div><h2>{p.title}</h2><p>{p.excerpt || p.body?.slice(0,160)}</p><span className="home-link">Read story <ArrowRight size={15}/></span></div></Link>)}</div>{!posts.length&&<div className="rounded-3xl bg-mist p-10 text-center"><h2>No stories yet</h2><p className="mt-2 text-stone">Publish your first article from the admin panel.</p></div>}</main></div>}
